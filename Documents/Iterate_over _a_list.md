# Iterate Over a List or Loop Through a List

The most common way to loop through data in a list is using the for command.

Example:

```
carros = ["gol", "celta", "palio"]

for carro in carros:
    print(carro)
```

